#include "holberton.h"

/**
 * swap - swap the values of two integers
 * @a: first param
 * @b: second param
 * Return: always zero
 */
void swap_int(int *a, int *b)
{
	int c = 98;

	*a = *b;
	*b = c;
}
