#include "holberton.h"

/**
 * main - prints its name
 * @argc: arguments count
 * @argv: arguments vector
 *
 * Return: On sucess always 0
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);

	return (0);
}
